import * as React from "react"

const Header = () => (
  <header
    style={{
      backgroundColor: '#222427',
    }}

    className="flex py-2 justify-center w-full"
  >
    <span className="text-white text-sm">Learn more about our latest features</span>
  </header>
)

export default Header
